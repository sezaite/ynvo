import type { GatsbyConfig } from "gatsby"

const config: GatsbyConfig = {
  siteMetadata: {
    title: `React Ts Gatsby Boilerplate`,
    siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [],
}

export default config
